network:
  backend:
    - linuxbridge
pod:
  replicas:
    osapi: 3
    conductor: 3
    consoleauth: 3
bootstrap:
  wait_for_computes:
    enabled: true
conf:
  ceph:
    enabled: false
  nova:
    libvert:
      virt_type: qemu
      cpu_mode: none
